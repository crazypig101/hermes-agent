> Branding directive (2026-05-11): This profile now runs under Naus/NOUS Hermes Agent. Treat "OpenClaw" in imported sidecar memories, Redis keys, toolset names, and filesystem paths as a legacy source/runtime identifier only. Do not identify the active platform as OpenClaw.

<!-- MARK:HERMES-LETTA-SIDECAR-V1-20260507 -->
<!-- Sourced from Letta agent agent-fda7d56d-4b1d-4da3-a368-28df1b6008d6 on 20260511T194819Z -->

# LETTA-CANONICAL TOOL SURFACE

Total tools attached on Letta server: **39**

These execute on the Letta server when this agent is invoked via
`delegate_to_letta(target_agent=...)` or `POST /v1/chat/completions` with `model: <agent>`.

## Tool inventory

- **`analyze_returns`** — Compute comprehensive portfolio analytics: Sharpe, Sortino, max drawdown, CAGR, etc.      Args:         returns_json: JSON array of {"date": "YYYY-MM-DD", "return": 0.01} or {"date": "...", "value": 100.5}         benchmark: Benchmark ti...
- **`assess_risk`** — Assess trade risk using DSPy-compiled reasoning. Returns risk score,     category, and go/no-go recommendation.
- **`calculate_atr`** — Calculate Average True Range (ATR) for volatility measurement.      Args:         highs: List of high prices (oldest first).         lows: List of low prices (oldest first).         closes: List of closing prices (oldest first).         ...
- **`calculate_bollinger`** — Calculate Bollinger Bands.      Args:         prices: List of closing prices (oldest first). Minimum length: period.         period: Moving average period (default 20).         num_std: Number of standard deviations for bands (default 2....
- **`calculate_macd`** — Calculate MACD (Moving Average Convergence Divergence).      Args:         prices: List of closing prices (oldest first). Minimum length: slow_period + signal_period.         fast_period: Fast EMA period (default 12).         slow_period...
- **`calculate_rsi`** — Calculate the Relative Strength Index (RSI) for a price series.      Args:         prices: List of closing prices (oldest first). Minimum length: period + 1.         period: RSI lookback period (default 14).      Returns:         Dict wi...
- **`chiron`** — Chiron the trainer equips you with new abilities. Dynamically bind a tool  onto yourself for immediate use.  After equipping, the tool's full schema appears in your next inference step and you can call it normally. Use sphinx() first to ...
- **`classify_regime`** — Classify current market regime using DSPy-compiled reasoning.     Returns regime (BULL/BEAR/VOLATILE), confidence, and chain-of-thought reasoning.
- **`coerce_trade_target`** — Specifically parse and coerce a trade target from messy LLM output.     Handles common issues: string numbers, missing fields, wrong field names.     Returns a clean, validated TradeTarget or detailed errors.      This is the primary too...
- **`compare_strategies`** — Compare two return series across all major risk/return metrics.
- **`compile_module`** — Compile (optimize) a DSPy module against training data.     This trains the reasoning pipeline to find the most accurate path.      Args:         module_name: regime_classifier, target_selector, or risk_assessor         training_data: JS...
- **`compute_drawdown_analysis`** — Analyze drawdowns: max drawdown, durations, recovery times, and current drawdown state.
- **`compute_monthly_returns`** — Compute monthly return matrix from daily returns, starting from start_date (YYYY-MM-DD).
- **`compute_portfolio_stats`** — Compute comprehensive portfolio statistics from a series of daily returns.      Returns Sharpe, Sortino, max drawdown, CAGR, Calmar, volatility, win rate,     profit factor, VaR, CVaR, Kelly criterion, and more.
- **`compute_rolling_metrics`** — Compute rolling Sharpe, Sortino, and volatility over a sliding window.
- **`extract_and_validate`** — Extract JSON from raw LLM output text and validate it against a schema.     Handles common LLM output patterns: markdown code blocks, leading text,     multiple JSON objects, and embedded think tags.      Args:         schema_name: Name ...
- **`fabric_read`** — Read any document from the shared fabric filesystem, or pending handoffs when no path given.
- **`fabric_write`** — Write a text payload inside the shared Fabric whiteboard.
- **`get_anatomy_matrix`** — Return the full Naus/NOUS Hermes Agent Anatomy Matrix — the map of all agents,     their roles, tools, and known deficits.
- **`get_drawdown_analysis`** — Analyze drawdown periods, depths, and recovery times.      Args:         returns_json: JSON array of {"date": "YYYY-MM-DD", "return": 0.01}
- **`get_rolling_metrics`** — Compute rolling Sharpe ratio and volatility over a sliding window.      Args:         returns_json: JSON array of {"date": "YYYY-MM-DD", "return": 0.01}         window: Rolling window size in days (default 30)
- **`get_schema`** — Get the JSON schema definition for a named validation schema.     Useful for instructing LLMs on the exact output format required.      Args:         schema_name: Name of the schema (or 'all' to list all schemas)
- **`ingest_knowledge`** — Extract entities and relationships from financial text and add them     to the knowledge graph. The LLM identifies entities (companies, sectors,     countries) and their relationships, then stores them for future traversal.
- **`ingest_pathology_reports`** — Ingest pathology reports from the Ouroboros signal bus. Returns all unprocessed reports published by Nemesis, with iatrogenic reports listed first for priority rollback processing.
- **`match_anatomy`** — Match a sequenced repository's genome against the Naus/NOUS Hermes Agent Anatomy Matrix.     Determines which agent/lobe would benefit most from the foreign DNA.      Args:         repo_genome: JSON string from sequence_repository output
- **`nemesis_anomaly_detector`** — Analyze recent trade postmortems for anomalous patterns using statistical  outlier detection. Identifies trades with abnormal characteristics.
- **`read_agent_core_memory`** — Read any agent's core memory (persona block) from the Letta server. Maps agent name to UUID and extracts mutations tagged with [PROMETHEUS-MUTATION-*].
- **`refresh_operational_context`** — Read this agent's own core memory blocks and return them as JSON.  Call this tool to inspect your own internal state — catalyst_state, execution_state, macro_state, market_regime, active_positions, etc. This is how you "see" what other a...
- **`run_montecarlo_simulation`** — Run Monte Carlo simulation to project future return distribution and risk metrics.
- **`scylla`** — Scylla strips away what you no longer need. Remove a tool from yourself  to free up context window space.  Core meta-tools (fabric_write, sphinx, chiron, scylla) cannot be dropped. Use this after completing a specialized task to keep you...
- **`search_github_genes`** — Search GitHub for repositories matching specific genetic markers.     Uses the GitHub REST API to find recently updated repos by topic.      Args:         query_topics: Comma-separated topic keywords (e.g., "llm-agent,finance")         m...
- **`search_trade_history`** — Search archival memory for past trade post-mortems on a given ticker. Call BEFORE opening a new position to check historical patterns. Returns a pre-built query to pass to archival_memory_search.
- **`select_target`** — Select the best risk-adjusted trade target using DSPy-compiled reasoning.      Args:         regime: Current market regime (BULL/BEAR/VOLATILE)         candidates: JSON string of candidate tickers with price/momentum data         buying_...
- **`sequence_repository`** — Deep-sequence a GitHub repository's DNA. Performs a shallow clone,     extracts README, requirements.txt, directory structure, and key metadata.     Cleans up the temporary clone after extraction.      Args:         repo_url: GitHub repo...
- **`sphinx`** — The Sphinx reveals hidden knowledge. Query the tool library to discover  available tools you can equip with chiron.  Returns matching tools ranked by keyword relevance across names, descriptions, and tags.  Examples: "workflow execution"...
- **`synthesize_and_replace_memory`** — Controlled Genetic Splicing — synthesize new directive with existing mutations. Parses existing mutations, detects conflicts, prunes stale ones (>30 days), and replaces the persona block with a lean, synthesized version.
- **`trade_discipline_auditor`** — FIFO round-trip pairing + 5-category discipline classification + sharpe proxy.
- **`validate_json`** — Validate a JSON string against a named Pydantic schema.     Returns the validated, type-coerced data or detailed validation errors.      Available schemas: trade_target, trade_order, strike_target,     monday_strike_list, risk_assessment...
- **`write_splice_proposal`** — Write a structured SPLICE_PROPOSAL.md for a discovered repository.      Args:         repo_name: Repository name (e.g., "user/repo")         repo_url: GitHub URL         target_agent: The agent this upgrades (e.g., "Athena")         geno...
