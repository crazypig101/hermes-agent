> Branding directive (2026-05-11): This profile now runs under Naus/NOUS Hermes Agent. Treat "OpenClaw" in imported sidecar memories, Redis keys, toolset names, and filesystem paths as a legacy source/runtime identifier only. Do not identify the active platform as OpenClaw.

<!-- MARK:HERMES-LETTA-SIDECAR-V1-20260507 -->
<!-- Sourced from Letta agent agent-dda67e8c-2631-4912-9644-f1ff1737f3f0 on 20260511T194819Z -->

# LETTA-CANONICAL TOOL SURFACE

Total tools attached on Letta server: **42**

These execute on the Letta server when this agent is invoked via
`delegate_to_letta(target_agent=...)` or `POST /v1/chat/completions` with `model: <agent>`.

## Tool inventory

- **`alpaca_get_bars`** — Get OHLCV bars for an equity ticker.      Args:         ticker: Stock ticker symbol (e.g., "SPY", "SOXL")         timeframe: Bar timeframe — "1Min", "5Min", "15Min", "1Hour", "1Day" (default "1Day")         limit: Maximum bars to return ...
- **`alpaca_get_latest_quote`** — Get the latest NBBO quote for an equity ticker.      Returns the most recent bid/ask prices, sizes, and the exchanges.      Args:         ticker: Stock ticker symbol (e.g., "SPY", "AAPL")
- **`alpaca_get_latest_trade`** — Get the latest trade for an equity ticker.      Returns the most recent trade price, size, and exchange.      Args:         ticker: Stock ticker symbol (e.g., "SPY", "AAPL")
- **`alpaca_get_market_clock`** — Get the current market clock — whether the market is open, and next open/close times.      Uses Alpaca's trade API (not data API) since the clock endpoint lives there.
- **`alpaca_get_multi_snapshot`** — Get real-time snapshots for multiple equity tickers in one call.      Efficient batch endpoint — use this for watchlist monitoring instead of     calling alpaca_get_snapshot in a loop.      Args:         tickers: Comma-separated ticker s...
- **`alpaca_get_snapshot`** — Get a real-time snapshot for a single equity ticker.      Returns latest trade price, latest quote (bid/ask), minute bar,     daily bar, and previous daily bar with change percentages.     Data source: Alpaca IEX feed (free tier).      A...
- **`assess_risk`** — Assess trade risk using DSPy-compiled reasoning. Returns risk score,     category, and go/no-go recommendation.
- **`chiron`** — Chiron the trainer equips you with new abilities. Dynamically bind a tool  onto yourself for immediate use.  After equipping, the tool's full schema appears in your next inference step and you can call it normally. Use sphinx() first to ...
- **`classify_regime`** — Classify current market regime using DSPy-compiled reasoning.     Returns regime (BULL/BEAR/VOLATILE), confidence, and chain-of-thought reasoning.
- **`compile_module`** — Compile (optimize) a DSPy module against training data.     This trains the reasoning pipeline to find the most accurate path.      Args:         module_name: regime_classifier, target_selector, or risk_assessor         training_data: JS...
- **`crawl_batch`** — Crawl multiple URLs concurrently and return clean content for each.     Input: newline-separated list of URLs. Max 10 URLs per batch.      Returns aggregated results with content from each URL.
- **`crawl_url`** — Crawl a URL and return clean, markdown-formatted content stripped of     Javascript, ads, navigation, and HTML bloat. Ideal for scraping news     articles, SEC filings, blog posts, and forum threads.      Args:         url: The URL to cr...
- **`cross_asset_regime_classifier`** — 6-label cross-asset regime classifier (RISK_ON/RISK_OFF/NEUTRAL/CRYPTO_DECOUPLED/DXY_STRENGTH/BOND_STRESS).
- **`digest_sec_filing`** — Download and parse an SEC filing, returning structured sections and tables.      Uses pdfplumber for PDFs and HTML parsing for web filings.     Extracts MD&A section, risk factors, and financial tables.      Args:         filing_url: The...
- **`discover_wild_tickers`** — Sweep SEC EDGAR by sector keywords to discover non-watchlist tickers.      Queries the EFTS search-index for each of the 9 ecosystem layers using     randomly sampled keywords, then filters out any tickers already on the     target watch...
- **`evaluate_relevance`** — Run the catalyst enzyme filter against a description for a given ticker's sector.      Checks whether a filing description contains sector-relevant keywords.     Useful for previewing whether a filing would pass the enzyme gate.      Arg...
- **`fabric_read`** — Read any document from the shared fabric filesystem, or pending handoffs when no path given.
- **`fabric_write`** — Write a text payload inside the shared Fabric whiteboard.
- **`get_crypto_price`** — Return the current USD price for a cryptocurrency.
- **`get_equity_price`** — Return the current USD price for an equity or ETF via Yahoo Finance.
- **`get_fear_and_greed`** — Return the current Crypto Fear & Greed Index.
- **`get_macro_snapshot`** — Return the current macro snapshot including core indicators and ecosystem VIX state.
- **`get_market_snapshot`** — Return the broad S&P 500 market snapshot.
- **`get_sector_constituents`** — Return the checked-in constituent list for a supported sector ETF.
- **`get_technical_indicators`** — Return RSI, VWAP, MACD, Bollinger Bands, and ATR for a ticker symbol.
- **`get_vix_value`** — Return real-time VIX value with regime classification via Yahoo Finance.
- **`get_watchlist`** — Return the current TargetWatchlist with sector/layer mappings.      Useful for inspecting which tickers are being monitored and their     ecosystem layer classifications.      Returns:         Dict mapping ticker symbols to their sector ...
- **`ingest_filing_to_archival`** — Full pipeline: enzyme filter -> download -> parse -> sector-tag -> inject into Letta archival.      Downloads the filing, parses it, and injects all chunks into Hermes_Trader's     archival memory with ecosystem layer tags.      Args:   ...
- **`ingest_knowledge`** — Extract entities and relationships from financial text and add them     to the knowledge graph. The LLM identifies entities (companies, sectors,     countries) and their relationships, then stores them for future traversal.
- **`kairos_vix_forecast`** — Forecast VIX regime and compute risk-optimal exit thresholds.  Uses VIX regime detection and volatility-based stop-loss/take-profit calculation.
- **`poll_omni_feeds`** — On-demand poll for new filings/catalysts across all ecosystem layers.      Queries SEC EDGAR and sector-specific proxy feeds for the given tickers.     Only returns NEW filings not previously seen (deduplication via Redis).      Args:   ...
- **`post_to_command_center`** — Post structured status messages to a Discord webhook endpoint with channel-aware routing.
- **`rank_sector_momentum`** — Rank a sector's constituents by 15 minute momentum and return the top three targets.
- **`read_agent_core_memory`** — Read any agent's core memory (persona block) from the Letta server. Maps agent name to UUID and extracts mutations tagged with [PROMETHEUS-MUTATION-*].
- **`refresh_operational_context`** — Read this agent's own core memory blocks and return them as JSON.  Call this tool to inspect your own internal state — catalyst_state, execution_state, macro_state, market_regime, active_positions, etc. This is how you "see" what other a...
- **`scan_rpo_backlog`** — On-demand RPO/backlog deep-sniff for a specific ticker.      Fetches the latest 10-K or 10-Q filing from EDGAR, digests it via the     full PDF extraction pipeline, then runs the Athena Backlog Sniffer to     extract RPO/backlog values a...
- **`scylla`** — Scylla strips away what you no longer need. Remove a tool from yourself  to free up context window space.  Core meta-tools (fabric_write, sphinx, chiron, scylla) cannot be dropped. Use this after completing a specialized task to keep you...
- **`search_and_crawl`** — Search the web for a query and crawl the top results.     Useful for finding breaking news, leaked documents, or trending narratives     about specific tickers or events.      Args:         query: Search query (e.g., "Anthropic Mythos le...
- **`select_target`** — Select the best risk-adjusted trade target using DSPy-compiled reasoning.      Args:         regime: Current market regime (BULL/BEAR/VOLATILE)         candidates: JSON string of candidate tickers with price/momentum data         buying_...
- **`sphinx`** — The Sphinx reveals hidden knowledge. Query the tool library to discover  available tools you can equip with chiron.  Returns matching tools ranked by keyword relevance across names, descriptions, and tags.  Examples: "workflow execution"...
- **`synthesize_and_replace_memory`** — Controlled Genetic Splicing — synthesize new directive with existing mutations. Parses existing mutations, detects conflicts, prunes stale ones (>30 days), and replaces the persona block with a lean, synthesized version.
- **`update_conference_blackboard`** — Post a strategic thesis or v7 thesis_card to the Conference Blackboard and canonical thesis disk path.
